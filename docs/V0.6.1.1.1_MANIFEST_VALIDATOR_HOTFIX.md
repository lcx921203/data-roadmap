# V0.6.1.1.1 Manifest Validator Hotfix

The V0.6.1.1 deployment failed in `validate:content`, not in TypeScript or build.

The knowledge rewrite preserved the same Manifest semantics but changed the
Chinese wording, while the repository validator still checks three exact
phrases.

This hotfix keeps the V0.6.1.1 Read Model and restores the exact P0 wording:

- 一个 Manifest 要么是 Data Manifest，要么是 Delete Manifest；
- 一个 Manifest 只对应一个 Partition Spec；
- Manifest 写出后就是不可变文件。

No workflow or unrelated content files are included.
