# V0.6.1.0.1 Frontstage Hotfix

Deploy V0.6.1.0 failed during `validate:content`.

Root cause:
several Iceberg chapters were rebuilt from an older local source snapshot while
their order/front matter was being updated. That reintroduced backstage editorial
copy such as `Project Fact Check`, `needs_fact_check`, `当前 V0.6.0`, and `本轮新增`
into visible Knowledge bodies.

This hotfix:
- removes those editorial/backstage sections from five affected Iceberg chapters;
- preserves the V0.6.1 11-chapter spine and new Row-level Changes chapter;
- preserves the Manifest correctness fixes and 8 MB / 100 merge semantics;
- fixes the Maintenance next-chapter transition for the new order.

No workflow files are included.
